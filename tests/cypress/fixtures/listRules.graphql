query {
    admin {
        jahia {
            clientCacheControl {
                rules {
                    priority,
                    methods,
                    urlRegexp,
                    header
                }
            }
        }
    }
}
